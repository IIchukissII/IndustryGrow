/*
 * SPDX-FileCopyrightText: 2026 The IndustryGrow contributors
 * SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * GENERATED by CMake from verify_key.h.in -- do not edit.
 *
 * The image-verification public key of ADR-0004 d14, given to the build as
 * -DIGROW_VERIFY_KEY_HEX=<128 hex chars> (an uncompressed P-256 point, X||Y,
 * without the 0x04 prefix). Custody and rotation of the private half belong to
 * the key ceremony (ADR-0024) and deliberately do not live in this repository.
 *
 * With no key given the array is zero and IGROW_VERIFY_KEY_PRESENT is 0, which
 * makes every verification fail closed.
 */
#ifndef IGROW_BOOT_VERIFY_KEY_H
#define IGROW_BOOT_VERIFY_KEY_H

#include <stdint.h>

#define IGROW_VERIFY_KEY_PRESENT @IGROW_VERIFY_KEY_PRESENT@

static const uint8_t igrow_verify_public_key[64] = {
@IGROW_VERIFY_KEY_BYTES@
};

#endif /* IGROW_BOOT_VERIFY_KEY_H */
